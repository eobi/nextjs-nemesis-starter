# Nemesis Shield app token. Get a free one (nsk_...) at https://shield.nemesislabs.xyz
# The app runs fine without it (the WAF is fail-open), but set it to start protecting.
NEMESIS_TOKEN=nsk_your_app_token
